// Analagous to Rails's db/seeds.rb file.  This is for priming the DB.

require("../config/application");

var Application = App.require("app/models/application");

// Create needed initial DB records here

App.shutdown();

